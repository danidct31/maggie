# IMPORTANT: In Railway → this service → Settings → Root Directory = web
[build]
builder = "NIXPACKS"
buildCommand = "(npm ci || npm install) && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
